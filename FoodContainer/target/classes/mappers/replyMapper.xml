<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
<mapper namespace="team.project.mapper.replyMapper" >

<select id="readReply" resultType="ReplyVO">
	SELECT * FROM reply where  del_yn = 'N' and reply_index =#{reply_index}
</select>
<insert id="writeReply">
INSERT INTO reply(
	recipe_index,
	member_index,
	contents
)VALUE(
	#{recipe_index},
	#{member_index},
	#{contents}
)

</insert>

 
</mapper>


