<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
<mapper namespace="team.project.mapper.cartMapper" >
 	
 	<insert id="cartInsert" parameterType="CartVO">
		INSERT INTO cart(product_index, member_index, cart_count, cart_date)
		VALUES(#{product_index}, #{member_index}, #{cart_count}, #{cart_date})
	</insert>
  
</mapper>